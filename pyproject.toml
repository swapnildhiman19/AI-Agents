[project]
name = "ai-agents"
version = "0.1.0"
description = "AI Agents workspace — learning ADK, LLMs, and agentic patterns"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-adk>=2.3.0",
    "python-dotenv>=1.1.0",
]
